---
const { title, value, color = "bg-white" } = Astro.props;
---
<div class:list={["p-6 rounded-2xl shadow-sm border border-slate-100", color]}>
  <p class="text-sm font-medium text-slate-500 mb-1">{title}</p>
  <p class="text-2xl font-bold text-slate-900">{value}</p>
</div>
