---
const links = [
  { href: '/dashboard', label: 'Dashboard', icon: '📊' },
  { href: '/dashboard/scan', label: 'Scan Baru', icon: '📸' },
  { href: '/dashboard/history', label: 'Riwayat', icon: 'clock' },
];
---
<aside class="hidden md:flex flex-col w-64 bg-white border-r border-slate-100 h-full">
  <div class="p-6">
    <span class="text-xl font-black text-slate-900">WARKOP.App</span>
  </div>
  <nav class="flex-1 px-4 space-y-2">
    {links.map(link => (
      <a href={link.href} class="flex items-center gap-3 px-4 py-3 text-slate-600 hover:bg-slate-50 hover:text-slate-900 rounded-xl transition-colors">
        <span>{link.icon}</span>
        <span class="font-medium">{link.label}</span>
      </a>
    ))}
  </nav>
</aside>
